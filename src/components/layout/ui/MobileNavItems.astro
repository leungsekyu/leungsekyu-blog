---
import { mobileNavItems as navItems } from '@data/nav-items'
---

{
  navItems.map((item) => (
    <li>
      <a href={item.url} class="poppins text-lg" aria-label={`访问 ${item.name}`}>
        {item.name}
      </a>
    </li>
  ))
}
